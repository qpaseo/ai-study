**개념**

- k, v cache (k, v 값을 캐시하는것)
  - llm은 문장을 생성할때 "안녕 나는 a야"를 만들때에 "안녕", "나는" "a야"... 이런식으로 하나하나 개산하여 만든다
  - 이 과정속에서 "나는"을 만들때는 "안녕"만 참고하면되지만 그 뒤에 단어는 앞에있는 모든단어("안녕", "나는")에 대한 연산을 다시해야한다.
  - 그래서 연산한 값을 저장하고 그 저장한 값을 불러와 반복된 연산을 하지 않게하는것이다
  - k는 단어의 키워드를, v는 단어의 의미(내용물) 역할을 한다.